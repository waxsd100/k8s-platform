apiVersion: v1
kind: Service
metadata:
  name: wax100-blog
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: wax100-blog
  ports:
    - name: http
      port: 80
      targetPort: 2368
