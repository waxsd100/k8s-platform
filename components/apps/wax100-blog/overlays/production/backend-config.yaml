apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: wax100-blog-backend-config
spec:
  healthCheck:
    # GhostはHTTP GETに対して301/308リダイレクトを返すため、TCPヘルスチェックを使用
    type: TCP
    port: 2368
  connectionDraining:
    drainingTimeoutSec: 30
