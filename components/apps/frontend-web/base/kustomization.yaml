apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: frontend-env-config
    literals:
      - APP_THEME=light
      - LOG_LEVEL=info
