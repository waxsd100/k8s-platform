apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 推奨値を出すだけの VPA（updateMode: Off）。CRD は GKE の垂直 Pod 自動スケーリング
# (terraform/gke.tf の vertical_pod_autoscaling) を有効にすると入る。
resources:
  - vpa.yaml
